

<div class="col-xs-12">
<%= form_for([@product, @product.reviews.new]) do |f| %>
<%= f.text_area :comment, row: 3, cols: "50", placeholder:"120 caractères maximum..." %>
<%= f.hidden_field :product_id, value: @product.id %>
<br><br>
 <%= f.submit "Commentez ce projet", class: "btn btn-success" %>

<% end %>
</div>

