<div class="row">
  <div class="col-md-3">
  <ul class="sidebar-list">
  <li class="sidebar-item"><%= link_to "Mes articles", products_path,
class: "sidebar-link active" %></li>
  </ul>
  </div>
  <div class="col-md-9">
    <div class="panel panel-default">
        <div class="panel-heading">Mes articles
        </div>
        <div class="panel-body">
        <% @products.each do |product| %>
          <div class="row">
            <div class="col-md-2">
            <%= image_tag product.photos[0].image.url(:thumb) if product.photos.length > 0 %>
            </div>
            <div class="col-md-7">
            <h4><%= product.name %></h4>
            </div>
            <div class="col-md-3 right">
            <%= link_to "Mettre à jour", edit_product_path(product), class: "btn btn-primary" %>
            </div>
          </div>
          <hr>
          <% end %>
        </div>
      </div>
     </div>
 </div>
