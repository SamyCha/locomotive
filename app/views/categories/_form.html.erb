<%= simple_form_for(@category) do |f| %>
  <%= f.error_notification %>

  <div class="form-inputs">
              <div class="form-group">
              <label>Nom dde la catégorie (max. 10 caractères)</label>
              <%= f.text_field :name, placeholder: "Services", class: "form-control" %>
            </div>
  </div>

  <div class="form-actions">
    <%= f.button :submit %>
  </div>
<% end %>
